from setuptools import setup, find_packages

setup(
    name="korengine",
    version="0.1.0",
    packages=find_packages(),  # Automatically detect the package folder
    install_requires=[],  # Add dependencies if needed
    author="skib",
    author_email="whalemate853@gmail.com",
    description="Kor engine on top",
)